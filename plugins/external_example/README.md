# external_example plugin

Demonstrates a custom notifier that lives outside `ai_deploy/`.

Usage:
```
cp -r plugins/external_example my-plugins/custom-notifier
ai-deploy run --plugins my-plugins
```
